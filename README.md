offshore_image_processes
========================

processes to automate the daily retrieval and return of list page images with their bg stripped. Process includes all ftp, db queries, image processing
